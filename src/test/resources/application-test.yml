spring:
  datasource:
    url: jdbc:h2:mem:rashod_mem_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  liquibase:
    drop-first: true